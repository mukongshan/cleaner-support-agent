spring:
  application:
    name: cleaner-support-agent-backend

  profiles:
    # 默认本地开发环境；如需切换可通过环境变量 SPRING_PROFILES_ACTIVE=prod
    active: ${SPRING_PROFILES_ACTIVE:dev}

  # 数据库（默认 MySQL；也可通过环境变量覆盖为 Postgres）
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/cleaner_support?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:123456}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    properties:
      hibernate:
        format_sql: true
    show-sql: ${SPRING_JPA_SHOW_SQL:false}

  servlet:
    multipart:
      max-file-size: ${SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE:20MB}
      max-request-size: ${SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE:25MB}

server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful
  error:
    include-message: always
    include-binding-errors: always

logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
    org.springframework.web: ${LOG_LEVEL_SPRING_WEB:INFO}
    org.hibernate.SQL: ${LOG_LEVEL_HIBERNATE_SQL:WARN}
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: ${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE:health,info}
  endpoint:
    health:
      show-details: ${MANAGEMENT_HEALTH_SHOW_DETAILS:when_authorized}

# 业务自定义配置（目前代码未使用，先预留，后续可直接接入）
app:
  cors:
    # 前端地址；多地址用逗号分隔，后续你实现 CORS 配置类时可读取这里
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
  ai:
    # 例如 Dify/OpenAI 之类的 key/base-url，后续实现时用环境变量注入
    base-url: ${APP_AI_BASE_URL:}
    api-key: ${APP_AI_API_KEY:}
  media:
    upload-dir: ${APP_MEDIA_UPLOAD_DIR:./data/uploads}

  image-recognition:
    # Qwen-VL API配置
    qwen-vl:
      api-url: ${QWEN_VL_API_URL:https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation}
      api-key: ${QWEN_VL_API_KEY:}
      model: ${QWEN_VL_MODEL:qwen-vl-plus}  # qwen-vl-max, qwen-vl-plus
      timeout: ${QWEN_VL_TIMEOUT:30000}    # 30秒超时
      max-retries: ${QWEN_VL_MAX_RETRIES:3}
    # 图片存储配置
    upload-dir: ${IMAGE_UPLOAD_DIR:./data/images}
    max-file-size: ${IMAGE_MAX_SIZE:10485760}  # 10MB
    allowed-formats: ${IMAGE_ALLOWED_FORMATS:jpg,jpeg,png,webp}
  jwt:
    # JWT密钥（生产环境请使用环境变量设置更安全的密钥）
    secret: ${APP_JWT_SECRET:cleaner-support-agent-secret-key-for-jwt-token-generation-2024}
    # Token过期时间（单位：秒，默认7天）
    expiration: ${APP_JWT_EXPIRATION:604800}

# Seafile 配置
seafile:
  server-url: ${SEAFILE_SERVER_URL:https://box.nju.edu.cn}
  repo-token: ${SEAFILE_REPO_TOKEN:8142382146993d9f7fc328074f73b28ab644efea}
  repo-id: ${SEAFILE_REPO_ID:9136e9de-f49e-45f6-9aff-9e3abe07a257}
  repo-password: ${SEAFILE_REPO_PASSWORD:}

---
spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
    org.springframework.web: ${LOG_LEVEL_SPRING_WEB:DEBUG}

---
spring:
  config:
    activate:
      on-profile: prod

server:
  # 生产建议由反向代理/网关设置；这里保留占位
  port: ${SERVER_PORT:8080}

logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
